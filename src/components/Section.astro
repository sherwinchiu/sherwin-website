---
export interface Props {
  id: string;
  title: string;
}
const { id, title } = Astro.props;
---

<section id={id} class="mt-20 sm:mt-24 scroll-mt-20">
  <h2
    class="text-xs uppercase tracking-[0.18em] text-[var(--muted)] font-medium mb-6"
  >
    {title}
  </h2>
  <slot />
</section>
